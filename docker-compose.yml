php:
    image: fpm-mysqli
    volumes:
      - .:/var/www/v1
web:
  image: nginx
  volumes:
   - .:/var/www/v1
   - ./default.conf:/etc/nginx/conf.d/default.conf
  ports:
   - "80:80"
  links:
      - php
